# Plan 078 -- Retrieval Relevance + Knowledge-Injection Quality

Status: Locked (epic-review passed 2026-07-24, 3 rounds) -- stories execute as written; deviations need STOP + amendment + re-gate.
Owner: nikki + claude
Date: 2026-07-24

Related:
- Backlog #400 (this epic's origin).
- Plan 072 (Persona + Knowledge Architecture) -- the deferred note that spawned this: "retrieval has no relevance floor (returns top-K regardless of similarity), which lets weak queries surface loosely-related chunks. Deliberately NOT patched here; promoted to its own backlog epic" (072 Epic wrap, "Followed-on / out of scope").
- Plan 075 (Judge, Regen, Safety) -- the WORLD-GROUNDED judge check reads `loreContextSummary`; cleaner injection is expected to lower its false-fail rate (and therefore regen frequency + latency).
- Strategy 001 -- complete; this is standalone post-strategy quality work, not a child epic.

---

## Why now

The retrieval pipeline injects the top-K lore chunks into every grounded turn regardless of how weakly they match the query. OpenAI's vector search already returns a normalized relevance score per chunk, but the runtime throws it away and injects whatever comes back. On a query where nothing in the wiki is truly relevant, the NPC still receives K loosely-related chunks labelled "Evidence" -- which (a) can push the model to reference world details that do not apply (a WORLD-GROUNDED judge failure -> a regen -> added latency), (b) wastes prompt budget, and (c) dilutes genuinely-strong chunks when a result set mixes one good hit with three weak ones.

This is cheap to fix and compounds with 075: fewer noisy injections -> fewer WORLD-GROUNDED false-fails -> less regen -> lower per-turn latency. Zero new API calls -- the score is already on the wire.

## Non-goals

- No new retrieval API calls, no re-ranking model, no local embeddings (the 071.3 deferred `EmbeddingsProvider` seam stays deferred).
- No change to ingest, chunking, or the vector index contents (that is backlog #419, incremental lore ingest -- separate).
- No dynamic per-turn K sizing beyond the existing `maxLoreResults` cap; the floor removes noise, the cap bounds volume, and they compose.
- No change to the persona-card load path (072.3) -- this epic touches only probabilistic retrieval (layer 3), never the deterministic persona load (layers 1-2).

## Current behavior (ground truth, verify at the gate)

- `RetrieveStage.execute` (`packages/plugins/src/catalog/sugaragent/runtime/stages/RetrieveStage.ts`) calls `vectorStoreProvider.searchLore({ maxResults: config.maxLoreResults, filters })`. It targets/broadens/pins purely by `page_id` (`retrievalFilters`, `broadenedBeyondLorePage`, `shouldPinNpcLore`). It NEVER reads `item.score` on real results -- the only place a score is set is the synthetic runtime-location evidence (`score: 1`, `RetrieveStage.ts:99`).
- The gateway forwards OpenAI's real per-chunk score: `score: typeof result?.["score"] === "number" ? result["score"] : 0` (`packages/plugins/src/deployment/gateway/core.ts:1256`). The search body is built at `core.ts:1240-1246` (`query`, `max_num_results`, opaque `filters`) -- no `ranking_options` today.
- `RetrievedEvidenceItem.score: number` exists on the type (`packages/plugins/src/catalog/sugaragent/runtime/types.ts:309`).
- `loreContext` is consumed at three sites: `GenerateStage.ts:246` (-> `summarizeEvidence` -> the "Evidence:" prompt block at `generate/prompt/builder.ts:239-240`), `PlanStage.ts:71` (`hasEvidence = loreContext.length > 0`) and `PlanStage.ts:138` (`claims`). The generate path's `loreContextSummary` is also handed to the Judge (`JudgeRequest.loreContextSummary`, `clients.ts:283`).
- Config: `maxLoreResults` default 4 (declared at `index.ts:490`, clamped 1..8 in the normalizer at `index.ts:244-248` and again in `runtime/clients.ts:140-143`), `maxLoreCharsPerItem` default 600. Both are Studio-settable plugin config today; the floor should join them. NEITHER is in `gatewayRuntimeConfigKeys` (`index.ts:444`) -- they are pure client-side knobs, and `loreRelevanceFloor` is too (D1), so it must NOT be added to the gateway keys.
- The gateway defaults a MISSING score to `0` (`core.ts:1256`: `typeof result["score"] === "number" ? ... : 0`). On real OpenAI hits the score is always present; a `0` therefore means "score field absent," and any floor `> 0` would drop such a chunk. This is expected/rare but noted so the floor filter treats absent-score as sub-floor deliberately, not by accident.
- OpenAI docs (verified 2026-07-24, developers.openai.com/api/docs/guides/retrieval): vector-store search `score` is a normalized cosine similarity `0.0-1.0`; a server-side `ranking_options.score_threshold` (0.0-1.0, default 0) is documented. Hybrid ranking (semantic + keyword via RRF); exact default-ranker math undisclosed. The absolute score scale is therefore corpus/ranker-dependent -- the floor value is an EMPIRICAL knob, not a universal constant. RISK (community-reported, not reproduced by us): the SAME query/chunk pair can score very differently across time (e.g. 0.9 one day, ~0.03 another) -- see D3's instability guard. Two community threads also report `ranking_options.score_threshold` being rejected on some endpoints despite the docs; this is why the server-side path stays deferred + probe-first (see Deferred), and the client-side floor is v1.

## Design decisions (epic-review ratifies)

- D1 -- The floor is a CLIENT-SIDE post-filter in `RetrieveStage`, on the score already returned, NOT the server-side `ranking_options.score_threshold`. Rationale: observability + testability. Client-side, we see every chunk's score and can log exactly what the floor removed (house norm: no silent caps -- log what was dropped); we can unit-test it with a stubbed provider and no live endpoint; and it needs no PROBE-FIRST verification of an unverified request param. The server-side threshold is a deferred payload optimization (see Deferred), not the v1 mechanism.

- D2 -- Observability ships BEFORE the floor (story order). You cannot tune a floor you cannot see, and the correct floor value is empirical (D-current-behavior: absolute scores are corpus-dependent). 078.1 surfaces per-chunk scores in `RetrieveStage` diagnostics + the existing dev handle FIRST; 078.2 adds the filter. This also makes 078.1 independently valuable (visibility into retrieval quality) even if the floor is never enabled.

- D3 -- The floor default is DISABLED (`0`), preserving today's behavior on deploy. It is a Studio-settable plugin-config knob (`loreRelevanceFloor`, 0..1) tuned live against the 078.1 diagnostics. Shipping enabled with a guessed constant would be tuning-blind against an unknown score scale; shipping the knob + the visibility to set it is the honest unit of work. (Revisit trigger for a non-zero default is in Deferred.) INSTABILITY GUARD (per the community-reported score volatility in Current behavior): because the same query/chunk can score very differently across time, a fixed floor risks silently abstaining ALL lore on a bad-scoring window. Mitigation, all in-scope: (a) default stays 0; (b) 078.1 logs scores for REPEATED identical queries so volatility is observable BEFORE anyone sets a non-zero floor; (c) 078.3's tuning recipe tells the author to set the floor conservatively LOW (just above the observed noise floor, not near the on-topic cluster) precisely because of this volatility. If volatility proves severe, the floor stays 0 and this epic still delivered value via the observability half (078.1).

- D4 -- The floor composes with the existing page_id paths without breaking them. IMPLEMENTATION PLACEMENT (load-bearing -- the natural "filter loreContext at the end" is WRONG and would eat the pin). `RetrieveStage` has two branches; the floor filters the PRIMARY / broadened search result in EACH, immediately, before any merge or slice -- named per branch because the anchors differ:
  - Branch A (`excludeOwnPage`, `278-284`): insert `.filter(score >= floor)` between the own-page `.filter` and the `.slice(0, maxLoreResults)` -- filter the other-lore remainder before the slice, else you slice-then-floor and under-fill.
  - Branch B (else, `287-325`): insert immediately after the broaden block (`303-307`), BEFORE the pin merge (`309`). Branch B's non-pin path has no own-page exclusion and no `.slice` -- do NOT hunt for one; the only slice lives inside the pin-merge block and must stay AFTER the filter so the pin bypasses it.
  Guard `floor` as a no-op when it is null/undefined/`<= 0` (write it `!(floor > 0)`, NOT `floor <= 0` -- `undefined <= 0` is `false` in JS and would skip the guard). The composition rules that fall out:
  - The synthetic runtime-location evidence (`score: 1`) is authoritative context, not retrieval roulette -- it always passes (a floor <= 1 never removes it; it is also prepended AFTER filtering at `RetrieveStage.ts:347`, so it is structurally exempt).
  - The pinned own-page chunk (`shouldPinNpcLore`, 072.6) is a deliberate identity anchor, not a relevance match -- because filtering happens BEFORE the pin merge, the pin BYPASSES the floor for free. Its score is logged (078.1) so a chronically-weak pin is visible.
  - Broaden-on-zero-results (`RetrieveStage.ts:304-307`) runs first; the floor applies to whatever the broadened search returns. If the floor then empties the set, that is a legitimate "no relevant lore this turn" outcome, NOT an error: `loreSearchPerformed` stays true, `status` stays `ok`, `loreContext` is simply empty, and downstream `hasEvidence` (`PlanStage`) correctly reads false. Nothing crashes -- `generate/prompt/builder.ts:241` already emits "Evidence: none retrieved." for the empty case.
  - DOWNSTREAM BEHAVIOR (corrected -- verify at gate): an empty evidence set does NOT make the NPC "answer from persona." For `lore_world`/`lore_other`/`identity_self`/`session_recall` intents, `planning.ts:181` sets `responseIntent = hasEvidence || memoryGrounds ? "answer" : "abstain"` -- so with no evidence and no memory/quest grounding the NPC ABSTAINS (a grounded "I don't know enough about that yet" turn, `helpers.ts` generic-abstain line), and `planning.ts:197-207` sets `responseSpecificity = "generic-only"`, which at `GenerateStage.ts:316-325` short-circuits to a DETERMINISTIC canned reply (no LLM) ONLY when `!personaLoaded || isOpeningTurn`; a persona-loaded mid-conversation NPC still routes to the LLM as a grounded-abstain. This is the DESIRED outcome (abstaining beats reciting weak lore) but must be described accurately so QA doesn't read the abstain as a regression.
  - The persona-exclusion path (072.6 `excludeOwnPage`) already drops own-page results client-side then slices to `maxLoreResults`; the floor applies to the OTHER-lore remainder, after the own-page `.filter` and before the `.slice`.

- D5 -- "Inject-only-relevant" is not a separate feature; the floor IS it. A chunk that clears the floor is relevant enough to inject; one that does not is dropped. No per-site injection logic changes -- every consumer reads the already-filtered `RetrieveResult.loreContext`, so filtering once in `RetrieveStage` fixes them all at the source (one source of truth). Concretely: `GenerateStage.ts:246` summarizes `loreContext` into the "Evidence:" prompt block, `PlanStage.ts:71` gates `hasEvidence` on `loreContext.length`, and -- stronger than the first draft claimed -- the Judge INDEPENDENTLY re-derives its own summary from the same `input.retrieve.loreContext` (`JudgeStage.ts:121`, `item.text.slice(0, 300)`); it does NOT reuse GenerateStage's variable. So the filter reaches the WORLD-GROUNDED judge check through the shared `loreContext`, not through any Generate->Judge handoff. This is the mechanism behind the "fewer judge false-fails" claim.

## Stories (EXECUTION ORDER)

### 078.1 Retrieval score observability (D2)

Surface the real per-chunk similarity scores so the floor can be tuned. Two parts, because there is NO existing retrieval dev handle to extend (verified: `__sugaragentQuestContext`/`__sugaragentMemory`/`__sugaragentPrompts` are each populated by their own middleware/registry; `RetrieveStage` has none):
(a) Add `loreScores` to the `RetrieveStage` diagnostics payload (the `createDiagnostics(...)` object at `RetrieveStage.ts:357-396`, right where `loreContextSummary` is already built) -- score per returned chunk, aligned to `loreContextSummary`, tagging each as retrieved / pinned / synthetic-location so the three sources are distinguishable. These already flow to `state.lastTurnDiagnostics`.
(b) Expose them live in preview. Prefer the CHEAP path: read scores off the already-exposed `lastTurnDiagnostics` (confirm the exposure point in code) rather than building new plumbing; only if that is not reachable in preview, add a `__sugaragentRetrieval` handle mirroring `quest-context-debug.ts`'s `record*Snapshot` + `install*DebugHandle` pattern. The story picks (b)'s sub-option after confirming what preview can already read.
For the instability guard (D3), the handle/diagnostics must make it possible to observe scores for the SAME query issued repeatedly (even if that is just "ask the same thing twice and compare the logged scores"). No behavior change to retrieval. Exit: unit test asserts scores flow into diagnostics for retrieved, pinned, and synthetic-location items, correctly tagged; preview smoke -- talk to a lore-backed NPC, read per-chunk scores, and confirm repeating one question surfaces its scores each time (volatility observable).

### 078.2 Relevance floor post-filter (D1, D3, D4, D5)

Add `loreRelevanceFloor` to the plugin config at ALL of these sites (mirror `maxLoreResults` EXACTLY except the clamp range and the gateway-key omission -- missing any one site = a silent no-op):
1. `runtime/types.ts` `SugarAgentPluginConfig` -- `loreRelevanceFloor: number` (near line 93).
2. `index.ts` normalizer -- clamped read `Math.max(0, Math.min(1, ...))` default `0` (clamp is 0..1, NOT maxLoreResults' 1..8).
3. `index.ts` `pluginSettingsSchema` -- a `number` field, min 0, max 1, default 0, in the same group as `maxLoreResults`, help copy noting 0 = off.
4. `index.ts` `defaultConfig` -- `loreRelevanceFloor: 0`.
5. Do NOT add to `gatewayRuntimeConfigKeys` (`index.ts:444`) -- it is a pure client-side knob (D1), and the schema validator (`packages/plugins/src/sdk/index.ts`, rule 5) only enforces the reverse direction (every gateway key needs a schema field, not vice versa), so a schema-only field is legal.
6. `retrieve-stage.test.ts` `makeContext().config` fixture -- add `loreRelevanceFloor` so the NEW floor unit tests (exit d/f) can set it. (It is NOT needed for compilation: the fixture is already passed `as never` at the sole call site and omits many required config fields; the add is for exercising the filter, not for the type-checker.)
7. Note: unlike `maxLoreResults` (re-clamped in `runtime/clients.ts:140-143` because it is sent to the gateway), the floor is read only in `RetrieveStage` from `context.config` and never crosses the client, so it needs NO `runtime/clients.ts` clamp -- do not add one.
Then in `RetrieveStage`, apply the floor at the PER-BRANCH PLACEMENT fixed in D4 (Branch A: between the own-page `.filter` and `.slice`; Branch B: after broaden, before pin merge), dropping results with `score < floor`, so pin + synthetic-location bypass for free. Guard the floor as a no-op with `!(floor > 0)` (handles null/undefined, not just `<= 0`). Log dropped count + dropped scores in diagnostics (no silent cap). Exit: unit tests -- (a) `floor=0` reproduces today's output exactly (byte-for-byte on a fixed stub), (b) a floor between two stubbed scores drops the weak chunk and keeps the strong one, (c) a pinned own-page chunk below the floor survives (bypass), (d) a floor above all retrieved scores yields empty `loreContext` with `status:ok` + `loreSearchPerformed:true` + a logged dropped-count, AND downstream `hasEvidence=false` routes a lore-intent turn to `abstain` / `generic-only` per D4 (assert the routing, not just status), (e) the synthetic runtime-location evidence survives any floor <= 1, (f) with `excludeOwnPage` active, the floor filters the other-lore remainder before the `.slice` (a weak other-page chunk is dropped, not just truncated away).

### 078.3 Wrap: docs + tuning note (D2, D3)

Update `docs/api` (the sugaragent knowledge-model doc -- `packages/plugins/src/catalog/sugaragent/docs/api/npc-knowledge-model.md`, per the 072 wrap) with: the floor config field, the score-observability handle/diagnostics, and a short tuning recipe that (a) reads scores across on- and off-topic questions, (b) checks the same question's score stability across repeats (the D3 instability guard), and (c) sets the floor conservatively LOW -- just above the off-topic noise, NOT near the on-topic cluster -- so day-to-day score drift does not silently abstain all lore. Document the empty-evidence downstream behavior explicitly (lore turns abstain / go generic-only) so it is not misread as a bug. Note the measured before/after on WORLD-GROUNDED judge-fail rate for one authored NPC if measurable. Backlog: file the server-side `score_threshold` payload optimization (Deferred below) as a task with its probe-first trigger.

## Verification recipe (nikki)

1. `pnpm test` green.
2. Preview a lore-backed NPC. Ask three ON-topic questions and three OFF-topic ones; read the per-chunk scores off the diagnostics/handle. Confirm off-topic turns show markedly lower scores than on-topic ones (validates the score is real and discriminating BEFORE any floor is set). Ask ONE question twice and compare its scores across the two asks (instability check per D3 -- if the same question's scores swing wildly, keep the floor at 0).
3. Set `loreRelevanceFloor` in Studio conservatively LOW (just above the observed off-topic noise, well under the on-topic cluster -- per D3, not near the on-topic scores); Save + Deploy Local Gateway. Re-ask: on-topic turns still inject evidence; off-topic lore turns now inject NO evidence and the NPC ABSTAINS gracefully ("I don't know enough about that yet"-style grounded turn) rather than reciting loosely-related lore -- this abstain is the intended win, NOT a regression. Nothing crashes; `status` stays ok.
4. Set the floor to `0`: behavior is exactly as before this epic (regression guard).
5. Set the floor absurdly high (e.g. `0.99`): every lore turn injects no evidence and abstains/emits a generic-only line (deterministic for a persona-less or opening turn, grounded-abstain LLM turn otherwise), no errors, diagnostics show a dropped-count each turn.

## Epic wrap

docs/api updated (floor field, score handle, tuning recipe). Before/after WORLD-GROUNDED fail-rate noted if measured. Backlog sweep incl. the server-side `score_threshold` follow-on.

## Deferred (with revisit triggers)

- Server-side `ranking_options.score_threshold` forward: once the client-side floor is calibrated (078 shipped + a floor value chosen), forward that value into the gateway search body (`core.ts:1240`) to stop OpenAI returning sub-floor chunks at all (payload trim). Revisit trigger: when retrieval response size or count of dropped chunks becomes a felt cost. PROBE-FIRST when picked up: `ranking_options.score_threshold` must be verified accepted (not 400'd) against the live OpenAI `/search` endpoint before relying on it -- same discipline as 072.6's `ne`-filter probe. Keep the client-side floor as the observable source of truth even if the server threshold is added (defense in depth + diagnostics).
- Non-zero default floor: revisit once a floor value proves stable across multiple authored worlds (not just wordlark) -- only then is a shipped default defensible. Until then the knob defaults off.
- Query-conditioned dynamic K / re-ranking: if noise persists ABOVE the floor (strong-but-wrong chunks), revisit the deferred 071.3 re-rank seam. Out of scope here; the floor addresses weak matches, not confidently-wrong ones.
