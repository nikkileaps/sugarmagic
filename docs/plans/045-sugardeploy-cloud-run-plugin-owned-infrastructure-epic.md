# Plan 045: SugarDeploy Cloud Run Plugin-Owned Infrastructure Epic

**Status:** Complete
**Date:** 2026-05-18 (proposed) / 2026-06-15 (accepted) / 2026-06-22 (complete)

> Architectural rules from this epic are captured in
> [ADR 017](/docs/adr/017-sugardeploy-cloud-run-architecture.md).
> Operating reference for the deploy plugin: [packages/plugins/src/deployment/README.md](/packages/plugins/src/deployment/README.md).
> Frontend / static-asset hosting for the playable game client is
> explicitly out of this epic's scope (see "What is NOT in scope" below)
> and will land in a follow-up plan.

## Epic

### Title

Complete the SugarDeploy Cloud Run target so that every artifact required to stand up, deploy, observe, rotate, and tear down a game's cloud infrastructure is generated and orchestrated by the plugin. No hand-written terraform, no per-game bespoke deploy scripts, no third repo, no CLI-only secret-setting workflow. The Studio SugarDeploy workspace becomes the single editing and operating surface for the full deployment lifecycle on Google Cloud Run.

### Goal

- **Plugin-generated terraform under the deployment directory.** The SugarDeploy plugin emits `main.tf`, `variables.tf`, `outputs.tf`, and `terraform.tfvars` into `<game-root>/deployment/google-cloud-run/terraform/` on every project save. Every file carries the existing `GENERATED BY SUGARMAGIC - DO NOT EDIT` header AND a new `# SUGARMAGIC TEMPLATE VERSION: NN` stamp. Terraform owns the **slow-changing surrounding infrastructure**: Artifact Registry repo, runtime service account, Workload Identity Federation pool/provider scoped to the configured GitHub repo, IAM bindings (run.admin, artifactregistry.writer, secretmanager.viewer for the GitHub WIF principal; secretAccessor for the runtime SA), and empty Secret Manager containers for vendor API keys. Terraform deliberately does **NOT** own the `google_cloud_run_v2_service` resource — the Cloud Run service lifecycle is `gcloud`-managed (see deploy and stop below). This avoids the wordlark-v1 awkwardness of declaring a placeholder service in terraform that CI immediately overrides via `gcloud run deploy`, and keeps fast-changing deploy state out of terraform's plan/apply loop.

- **Studio fields back every terraform input.** The SugarDeploy workspace's Cloud Run section grows fields for `gcp_project_id`, `region`, `github_repo`, `service_name_prefix`, and any other terraform variables. Values persist in `gameProject.deployment.targetOverrides["google-cloud-run"]` (same path the existing overrides already use). No `.tfvars` hand-editing is part of the supported workflow.

- **Local terraform state, gitignored.** State lives at `<game-root>/deployment/google-cloud-run/terraform/terraform.tfstate` alongside `.terraform/`. Both are gitignored. No GCS backend bootstrap problem for solo dev. A remote-backend opt-in is a follow-up epic, not part of this one.

- **Same gateway scaffolding shared across local and Cloud Run targets.** Verify and document that `buildGatewayScaffoldFiles(targetId, unit, port)` produces identical Dockerfile / server / package / routes output for `local` and `google-cloud-run`. Eliminate any subtle divergence. The Cloud Run service runs the same `node server.mjs` proxy the local Docker container runs — no bundled local LLM, no bundled embeddings, no second runtime path.

- **Cloud Run deploy script consumes terraform outputs.** The generated `deploy.sh` reads Artifact Registry URL, runtime SA email, and Secret Manager secret names from `terraform output -json`. The script itself owns the Cloud Run service lifecycle via `gcloud run deploy` (idempotent — creates on first run, updates on subsequent runs). Secrets attach via `--set-secrets=KEY=SECRET_NAME:latest`, not `--set-env-vars`. Non-secret config (model names, vector store id, etc.) attach via `--set-env-vars`.

- **Studio-driven secret value management.** The SugarDeploy workspace's Cloud Run section grows a Secrets subsection listing every secret declared in the deployment plan. Each secret row shows a status (Set / Not set, last-updated timestamp + version id) and a Set Value action. Set Value opens a password-masked modal; submit sends a new host action `setSecretValue` to the Studio dev server middleware, which shells `gcloud secrets versions add` with the value passed via stdin. Values never enter studio state, never log, never persist beyond the gcloud invocation.

- **Stable Secret Manager naming convention.** Secret Manager secret names follow `${serviceNamePrefix}-${secretKey}` after slugifying, derived deterministically from the deployment plan. Env var name (e.g. `SUGARMAGIC_ANTHROPIC_API_KEY`) stays independent of the secret name (e.g. `wordlark-gateway-anthropic-api-key`) — bound through a `mappingHint` declared in the plan so renames become tractable via terraform `moved {}` blocks. Per-plan scoping (one set of secret values shared across all service units in a plan) is the v1 default; per-unit scoping is a future extension that prepends the service unit id when needed.

- **Lifecycle host actions for the full loop.** Extend the existing `/__sugardeploy/action` middleware (`apps/studio/vite.config.ts`) with action kinds: `setup-infra` (terraform apply), `teardown-infra` (delete Cloud Run service first via `gcloud run services delete --quiet`, THEN `terraform destroy` — ordering is required because terraform doesn't know about the service but its IAM/WIF bindings are referenced by it), `set-secret-value`, `secret-status`, plus the existing `deploy` / `status`. Implement `stop` for Cloud Run as `gcloud run services delete <service-name> --quiet` (mirrors `docker compose down` for the local target — service gone, surrounding infra and image remain so a subsequent `deploy` is fast). Implement `health` for Cloud Run as a real HTTP probe (not just a returned URL) against the deployed service URL retrieved via `gcloud run services describe`; upgrade the local target's `health` to perform an actual probe too, so both targets behave symmetrically. Each action is bound to a Studio UI affordance in the SugarDeploy workspace.

- **Template-version drift handling.** Plugin records the current template version it emits. On regeneration, if the existing artifacts on disk carry an older version, the plugin emits terraform `moved {}` blocks for any resource renames between versions so `terraform plan` stays non-destructive for existing games.

- **One GCP project per game per environment, documented as canonical.** The plugin-generated README and the SugarDeploy workspace copy state explicitly that cross-game resource sharing inside one GCP project is not supported. `gcloud projects delete` is the canonical nuclear cleanup; `terraform destroy` is the canonical fine-grained cleanup.

- **Game-project versioning via `majorVersion` metadata + git tags + version-namespaced GCP projects.** Each game project carries a `majorVersion: number` field in its top-level metadata. The deployment plugin's default `gcp_project_id` and `service_name_prefix` are templated as `${gameSlug}-v${majorVersion}` (override field still wins for the wordlark-style "reuse the existing GCP project" case). Cutting a new major version is a first-class Studio affordance: a **Cut New Major Version** button performs a sequenced `git tag` + metadata bump + `git commit` so the old major is anchored in git history and subsequent deploys land in a fresh GCP project. The old version's GCP project keeps running untouched because nothing redeployed it. Patching an old major is `git worktree add` against the old tag → open that worktree in Studio → its metadata says the old `majorVersion` → SugarDeploy targets the old GCP project automatically. One directory, one repo, one source of truth (git), N coexisting deployed versions.

### Why this epic exists

Earlier game projects shipped hand-written terraform alongside the game code. The engine evolved; the per-game terraform did not. Drift between engine expectations and game-root infrastructure produced parity bugs and stale cloud resources that this team has lived through (the wordlark-v1 to wordlark migration is the immediate trigger). Plan 021 and ADR 009 established the principle that Sugarmagic owns generated deployment artifacts even when they live in the game root. The local target already proves this works (plugin-generated Dockerfile, `server.mjs`, `docker-compose.yml`, all overwritten on save, no drift possible). This epic extends the same property to Cloud Run infrastructure — the highest-friction drift surface in the old setup.

The existing `google-cloud-run` target generates the gateway scaffold, a `deploy.sh`, a `service.yaml`, and a README, but assumes the GCP-side infrastructure (Artifact Registry, IAM, WIF, Secret Manager) already exists. The user fills in those gaps by hand today; that hand-work IS the drift surface. Closing it makes Cloud Run targets reproducible and disposable per game.

The decision to keep the same Dockerfile and gateway across local and Cloud Run (no bundled local LLM/embeddings in the cloud image) preserves AGENTS.md's "single enforcer / one type per behavior" rule. Bundling models for cloud and proxying for local would be exactly the kind of split-brain architecture this project exists to prevent.

The decision to drive secret values from the Studio (not CLI-only) follows the same single-input-surface logic that already governs Working Directory, project ID, and region: one place to edit, one validation path, one mental model.

The decision to support fine-grained (`terraform destroy`) and nuclear (`gcloud projects delete`) cleanup paths together — under a one-project-per-game/env discipline — directly addresses the existing stale-infra problem the team is currently living with.

### What is NOT in scope

- AWS Fargate or other non-Cloud-Run hosted targets (separate future epic per target)
- Multi-environment (staging vs production) per game — single environment per game project for v1; multi-env is a follow-up epic that may layer on `deployment.environmentOverrides` or move to a per-env subdirectory layout
- GitHub Actions workflow generation — the plugin's `deploy` action invoked from the Studio is the supported deploy path for v1; CI generation can land as a follow-up
- Frontend / Netlify deploy — out of scope; the Cloud Run target covers the backend gateway only
- Remote terraform state backend (GCS) — local state only for v1; remote-backend opt-in is a separate follow-up
- Custom domain / DNS / load balancer setup — Cloud Run-provided `*.run.app` URL only for v1
- A migration tool to import existing wordlark-v1-style infrastructure into the new plugin-generated terraform — wordlark-v1 stays as reference; new games start clean

## Deliverables

### Domain and plugin

- new terraform template generators under `packages/plugins/src/deployment/`:
  - `buildCloudRunTerraformMainFile(plan, overrides)` → `main.tf`
  - `buildCloudRunTerraformVariablesFile(plan, overrides)` → `variables.tf`
  - `buildCloudRunTerraformOutputsFile(plan, overrides)` → `outputs.tf`
  - `buildCloudRunTerraformTfvarsFile(plan, overrides)` → `terraform.tfvars`
- update `buildGoogleCloudRunManagedFiles` (`packages/plugins/src/deployment/index.ts`) to call the new generators and emit them under `deployment/google-cloud-run/terraform/`
- refactor `formatCloudRunDeployScript` so `deploy.sh` reads `terraform output -json` for `artifact_registry_url`, `runtime_sa_email`, and secret names; rewrite secret env-var passing to use `--set-secrets=...:latest`
- extend `normalizeGoogleCloudRunDeploymentTargetOverrides` (and the underlying type in `packages/domain/src/deployment/`) with new fields: `githubRepo` (persisted exactly as `owner/repo` — matches the GitHub OIDC `repository` claim so terraform IAM principals and `attribute.repository == "owner/repo"` conditions work with no transformation), optional `runtimeServiceAccountName`, anything else terraform needs
- add `majorVersion: number` to the top-level game project metadata type in `packages/domain/src/project/` (default to `1` when reading projects that lack the field — zero-effort migration for existing wordlark and any other in-flight projects). Expose it on `GameProject` so the plugin and Studio can read/write it through the existing project-loading path
- update the Cloud Run terraform/deploy generators so the default `gcp_project_id` and `service_name_prefix` are computed as `${gameSlug}-v${majorVersion}` whenever the user has not set the corresponding override. The override field continues to win when present (escape hatch for "reuse an existing arbitrarily-named GCP project" like the legacy `wordlark` project). Both `resolveSecretManagerName` and the deploy-script generator consume the resolved (overridden-or-defaulted) `serviceNamePrefix` — they do not derive version from elsewhere
- add a `cutNewMajorVersion(gameRoot)` helper in `packages/plugins/src/deployment/` (or a more general project-lifecycle module if one already fits) that performs the sequenced operation: (1) pre-flight checks — `git` on PATH, working tree clean, the tag `v${currentMajor}.0.0` does not already exist; (2) `git tag v${currentMajor}.0.0` at current `HEAD` (anchors the last commit of the old major); (3) bump `majorVersion` to `currentMajor + 1` in the project metadata file and write it; (4) `git add <metadata-file>` + `git commit -m "chore: bump major version to ${newMajor}"`. On any failure mid-sequence, roll back: delete the tag created in step 2 (`git tag -d`), `git checkout -- <metadata-file>` to discard the write from step 3. Push is **not** automatic — solo dev pushes their own tag/commit when ready
- add template version stamping helper + `moved {}` emission discipline; document the convention in `packages/plugins/src/deployment/README.md`
- add a `TERRAFORM_RENAME_LEDGER` constant in `packages/plugins/src/deployment/` as the source of truth for cross-version resource renames: `Array<{ fromVersion: number, toVersion: number, moves: Array<{ from: string, to: string }> }>`. On every regeneration the plugin walks the ledger from the on-disk version up to the current version and emits the union of `moved {}` blocks needed into `main.tf`. For v1 of this epic the ledger is empty (no prior versions exist); all entries are added in future epics when renames actually happen. The ledger retains entries indefinitely for v1 — a future plan may cap retention once the version count makes the generated terraform unwieldy
- expose a small read helper `readTemplateVersionFromDisk(gameRoot)` that parses the `# SUGARMAGIC TEMPLATE VERSION: NN` stamp from the existing `main.tf` (if present); returns `null` when no terraform has been generated yet. The Studio uses this to drive the drift banner described in the Studio UI section
- add a shared helper `resolveSecretManagerName(plan, secretKey)` in `packages/plugins/src/deployment/` that produces `${serviceNamePrefix}-${secretKey}` with these rules: lowercase the input, replace any char NOT in `[a-z0-9-]` with `-`, collapse consecutive `-`, trim leading/trailing `-`, and if the result starts with a digit, prepend `s-` (GCP Secret Manager names cannot start with a digit). If `serviceNamePrefix` is empty, fall back to the game project slug; if neither is resolvable, throw a plan-time error. Cap the final name at 80 characters and throw a clear error if exceeded (GCP allows 255 but CLI/Cloud Run references get unwieldy past ~80). This helper is the single source of truth used by the terraform generator, the deploy script generator (for `--set-secrets` flags), and the `set-secret-value` / `secret-status` host actions — those four call sites must not derive the name independently or they'll drift

### Studio UI

- extend the SugarDeploy workspace's Cloud Run section (`apps/studio/src/plugins/catalog/sugardeploy/index.tsx`) with input fields for the new override values
- the GitHub Repository field accepts and persists `owner/repo` form only; validate against `^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$`; be forgiving on paste-in by stripping a leading `https://github.com/` or `git@github.com:` and a trailing `.git` before validation; placeholder `owner/repo` (e.g. `nikki/wordlark`); help text explains the value drives Workload Identity Federation binding
- add a Secrets subsection that lists each declared Secret Manager secret with status + Set Value affordance
- add Studio buttons for Setup Infra, Teardown Infra, Set Secret, Refresh Secret Status, Stop, Health; bind each to the corresponding host-action kind
- ensure the Set Value modal masks input, never logs, never retains the value in component state after submit
- add a non-blocking **template-drift banner** to the SugarDeploy Cloud Run section: when `readTemplateVersionFromDisk(gameRoot)` returns a version strictly less than the plugin's current template version, render an info-styled banner above the action buttons reading roughly *"This game's terraform was last generated at template version X. Current is Y. The plugin will emit terraform `moved {}` blocks to keep the upgrade non-destructive — run Setup Infra (or `terraform plan` manually) and verify the diff before applying."* The banner clears automatically on the next successful save / Setup Infra run because the on-disk stamp updates to current; no explicit dismiss control
- ~~add a non-blocking **shared-project warning banner**~~ **DEFERRED.** Studio currently loads one project at a time; there is no live multi-game workspace state. Implementing cross-project detection via the IndexedDB-stored handle list would require permission re-prompts and per-render file reads. Re-scope to a future epic alongside actual multi-game workspace support. Cross-game discipline is still enforced by documentation (plugin-generated README + workspace copy)
- surface the current **`majorVersion`** globally in the Studio header (`apps/studio/src/App.tsx`) next to the project's `displayName` — when a project is loaded, the existing "📁 Game" menu trigger renders as `📁 ${displayName}` with a small `v${majorVersion}` `Badge` immediately after it; when no project is loaded, only the "Sugarmagic" brand is shown. The version is a project-level property and belongs in a project-level UI affordance, NOT buried inside the SugarDeploy section. The SugarDeploy section does not duplicate the version display
- add a **Cut New Major Version** button to the SugarDeploy Cloud Run section. Click → confirmation modal that shows: (a) the tag that will be created (`v${currentMajor}.0.0`), (b) the new `majorVersion` value, (c) the new default `gcp_project_id` after the bump (e.g. "Future deploys will go to `wordlark-v2` unless you override below"), (d) explicit copy that this action commits to git. Confirm → POST to the `cut-major-version` host action. Render success with the new `majorVersion` and the new project name; on failure show the structured error verbatim. Disable the button when: the project has uncommitted changes (must be clean for the tag to be meaningful), or git is not on PATH. Show the disabled reason in a tooltip so the user knows what to fix

### Host actions (Studio dev server middleware)

- extend `createSugarDeployHostPlugin` (`apps/studio/vite.config.ts`) with action kinds: `setup-infra`, `teardown-infra`, `set-secret-value`, `secret-status`, `stop`, `health`
- update `resolveDeploymentActionFromSettings` (`packages/plugins/src/deployment/actions.ts`) to return supported descriptors for `stop` and `health` for Cloud Run
- `stop` for Cloud Run resolves to `gcloud run services delete <service-name> --quiet --project=<projectId> --region=<region>`; idempotent (succeeds with a clear "already deleted" message if the service doesn't exist)
- `teardown-infra` is implemented as a sequenced wrapper: first runs `gcloud run services delete --quiet` for every Cloud Run service the deployment plan declares (each tolerating "not found"), then runs `terraform destroy -auto-approve` in the terraform directory. The ordering is required because terraform does not own the Cloud Run service resource but its IAM/WIF bindings are referenced by it; destroying those bindings while the service is still up causes dependency errors
- `health` performs an actual HTTP probe and returns the result. The middleware does `fetch(healthUrl, { signal: AbortSignal.timeout(5000), redirect: "manual" })` and returns `{ ok, statusCode, durationMs, bodyPreview (first 512 bytes, utf-8), error? }`. No retries — one shot. Body is truncated because gateway responses can be arbitrarily large and we only want a smoke signal. Redirects are not followed because health probes should hit `/health` directly; any redirect is misconfiguration we want surfaced. Result is serialized into the existing action descriptor's `stdout` field so the Studio UI can render it the same way it renders other action results
- `health` for **Cloud Run** resolves the service URL once via `gcloud run services describe <service-name> --format='value(status.url)'`, then probes `<url>/health`. If `describe` returns no URL (service not deployed), report `supported: false` with reason "Service is not deployed" — do not attempt the probe
- `health` for **local** uses the already-known `http://localhost:<gatewayHostPortBase>/health`; no resolution step. If the gateway container is down, the probe fails with `ECONNREFUSED` and the error message surfaces directly — no special handling needed. Replaces the current "return healthUrl only" behavior so both targets behave symmetrically
- `set-secret-value` handler passes the value via stdin to `gcloud secrets versions add`; explicit log suppression for request/response on that endpoint; reject the request if `value` length is zero or absurdly large
- enforce that `setup-infra` and `teardown-infra` only run when a `terraform` binary is on PATH; emit a clear error otherwise
- add a `cut-major-version` action kind: invokes the `cutNewMajorVersion(gameRoot)` helper described in the Domain section. Pre-flight is enforced server-side too (not only in the UI's disabled state): refuse with a structured error if `git` is not on PATH, working tree is dirty, or the would-be tag already exists. On success return `{ previousMajorVersion, newMajorVersion, tagCreated, commitSha }` so the Studio can render confirmation and refresh the SugarDeploy section without a manual reload

### Documentation

- new ADR 017 capturing the underlying decisions (separate from this plan, recorded as architectural truth)
- update Plan 021 to reference this epic as the Cloud Run implementation pass and to reference ADR 017 as the authoritative design
- update `packages/plugins/src/deployment/README.md` with the template-version + moved-block discipline
- update the plugin-generated `deployment/google-cloud-run/README.md` to cover the new lifecycle: `terraform.tfvars` is generated, do not hand-edit; use the Studio for secret values; **one GCP project per game per environment is the supported model — sharing a project across games is not supported and breaks both cleanup paths (`terraform destroy` may remove bindings another game depends on; `gcloud projects delete` annihilates every game in the project simultaneously)**; `terraform destroy` for fine-grained cleanup, `gcloud projects delete` for nuclear cleanup; the Secret Manager naming convention (`<service-prefix>-<secret-key>`) so users grok the names they see in the GCP console and `gcloud secrets list` output; the **version-namespaced project naming** convention (`${gameSlug}-v${majorVersion}`) and the **Cut New Major Version** workflow (Studio button → git tag + metadata bump + commit; old major keeps running in its old GCP project; patching an old major uses `git worktree`)

### Tests

- unit tests for each terraform generator: snapshot test of the rendered file given a representative plan + overrides
- integration test for the host-action endpoints: `setup-infra` resolves to `terraform apply` with the correct cwd; `set-secret-value` resolves to `gcloud secrets versions add` with stdin payload; assert no value appears in the logged command
- a lint or test that verifies the dev server middleware's secret-handling endpoint does not write payload values to logs
- unit tests for `cutNewMajorVersion`: clean tree happy path, dirty tree refusal, existing-tag refusal, missing-git refusal, mid-sequence rollback (simulate metadata-write failure and assert the tag is deleted and no commit was made)

## Verification

This epic is complete when, on a fresh GCP project with `gcloud auth application-default login` and `terraform` available:

1. A user creates a new game in the Studio, fills in the Cloud Run section's fields, saves the project, and the `deployment/google-cloud-run/terraform/` directory contains valid generated terraform with the GENERATED header and template-version stamp.
2. Clicking Setup Infra in the SugarDeploy workspace runs `terraform apply` successfully and produces Artifact Registry, runtime SA, IAM bindings, WIF pool/provider, and empty Secret Manager containers.
3. Clicking Set Value on each Secret Manager secret successfully creates a new version with the provided value, and the secret value never appears in any Studio log, browser console, or shell history.
4. Clicking Deploy builds the gateway image, pushes it to Artifact Registry, and deploys the Cloud Run service with `--set-secrets` references resolving correctly at startup.
5. The deployed service responds to the gateway's `/health` endpoint and accepts SugarAgent calls from a separately-deployed (or local) Studio. Clicking Health in the SugarDeploy workspace returns a real probe result (`ok: true`, `statusCode: 200`, `durationMs`, truncated body) — not just a URL to copy.
6. Clicking Stop removes only the Cloud Run service (`gcloud run services delete`); Artifact Registry, IAM, WIF, and Secret Manager state remain. A subsequent Deploy brings the service back without re-running Setup Infra.
7. Clicking Teardown Infra deletes any remaining Cloud Run service(s) first, then runs `terraform destroy` cleanly without dependency errors, removing everything the plugin created.
8. Deleting the GCP project entirely (`gcloud projects delete`) removes all remaining state with no manual cleanup required.
9. With v1 deployed and running, clicking **Cut New Major Version** with a clean working tree creates the `v1.0.0` git tag, bumps the project metadata to `majorVersion: 2`, and commits the bump. The SugarDeploy section's default `gcp_project_id` now resolves to `${gameSlug}-v2`. Clicking **Create GCP Project + Setup Infra + Deploy** from this state stands up v2 in a fresh GCP project; v1 continues running in its original GCP project untouched. Checking out the `v1.0.0` tag in a `git worktree`, opening that worktree in Studio, and confirming SugarDeploy resolves back to the v1 GCP project demonstrates the patch-an-old-major path works.

## Stories

Stories are ordered for solo-dev linear execution. The Deliverables section above is the master detail; per-story descriptions stay light and lean on Deliverables by topic rather than restating them. Each story has an explicit **Exit** criterion that leaves the codebase in a working, verifiable state.

Dependency shape: `45.1 → 45.2 → 45.3 → 45.4 → 45.4.5 → 45.4.6 → 45.4.7 → 45.5 → 45.5.5 → 45.5.7 → 45.5.8 → 45.6 → 45.9`, with `45.7` parallel-after `45.2`, `45.7.5 → 45.8` (the migration must land before the cut flow can build on a clean shape), and `45.8.5` after `45.6` (the lifecycle UX overhaul iterates on the working deploy+health+stop loop). Cut New Major Version chains into the Create GCP Project + Setup Infra buttons that 45.4.5 + 45.4 land, but the host-action surface those buttons hit must already have been pushed inside the SugarDeploy plugin per 45.4.6, and the version-suffix system per 45.4.7 is what gives each major's project id global uniqueness. The baseline gateway from 45.5.5 is what makes 45.6's Health probe always have something to query, and 45.5.7 is what makes that gateway publicly reachable (until [Plan 047](047-identity-provider-plugin-model-epic.md) ships real identity-provider plugins).

### 45.1 — Project-versioning foundation

Adds `majorVersion: number` to the top-level game project metadata type in `packages/domain/src/project/`, defaulting to `1` for projects that lack the field (zero-effort migration for in-flight projects like wordlark). Surfaces it on `GameProject` through the existing project-loading path so the deployment plugin and Studio can read it without a special-case fetch. Updates the deployment plugin's default `gcp_project_id` and `service_name_prefix` derivation to `${gameSlug}-v${majorVersion}` — override fields continue to win when present. Updates the existing `deploy.sh` and `service.yaml` generators so the resolved names flow through the same defaulting path, avoiding inconsistency across files emitted in different stories.

Also surfaces the project identity globally in the Studio header — because `majorVersion` is a project-level property, not a deployment-specific one, it belongs in a project-level UI affordance and not buried inside the SugarDeploy section. The Studio header's bare "📁 Game" menu button is replaced (when a project is loaded) with the project's `displayName` plus a small `v${majorVersion}` `Badge` next to the 📁 menu trigger, keeping the menu behaviour unchanged. When no project is loaded, only the "Sugarmagic" brand is shown.

**Exit:** opening wordlark in the Studio loads it with `majorVersion: 1`; saving with no overrides set regenerates the existing `deploy.sh` and `service.yaml` with version-namespaced names — the resolved `gcp_project_id` and `service_name_prefix` are both `wordlark-v1`, and the final Cloud Run service name (prefix + service-unit id) carries `wordlark-v1-` as its prefix. The Studio header shows `📁 Wordlark  v1` when wordlark is the active project. `resolveSecretManagerName(plan, "anthropic-api-key")` will return `wordlark-v1-anthropic-api-key` once 45.5 lands (the helper itself is implemented in 45.5; the version-derived prefix is locked in here).

### 45.2 — Cloud Run terraform generators

Implements the four terraform generators (`buildCloudRunTerraformMainFile`, `buildCloudRunTerraformVariablesFile`, `buildCloudRunTerraformOutputsFile`, `buildCloudRunTerraformTfvarsFile`) and wires them into `buildGoogleCloudRunManagedFiles` so saving a project emits the files under `deployment/google-cloud-run/terraform/`. Adds the template-version stamping helper, the (empty) `TERRAFORM_RENAME_LEDGER`, and `readTemplateVersionFromDisk`. Adds `.gitignore` entries for `terraform.tfstate` and `.terraform/`. No Studio UI changes — the generators consume whatever overrides exist plus the 45.1 defaults.

**Exit:** saving wordlark produces a populated `deployment/google-cloud-run/terraform/` directory; `cd` into it + `terraform init` + `terraform validate` succeeds against a real GCP project (no `apply` yet); the generated `main.tf` carries both the `GENERATED BY SUGARMAGIC` header and a `# SUGARMAGIC TEMPLATE VERSION: 1` stamp; `terraform.tfstate` and `.terraform/` are gitignored.

### 45.3 — Studio fields

Extends the SugarDeploy Cloud Run section UI with input fields for `gcp_project_id`, `region`, `github_repo` (with paste-forgiveness for `https://github.com/owner/repo` and `git@github.com:owner/repo.git` forms, validation against `^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$`), `service_name_prefix`, and `runtime_service_account_name`. Fixes the form binding so each field displays the **raw override value** (so blank stays blank visually) with the auto-derived default rendered as a `placeholder`. Adds help text under each field explaining what blank means. Pure data-binding + visual affordance — no new host actions. The version display is a global header element (landed in 45.1), so the SugarDeploy section does not duplicate it.

**Shared-project warning banner is deferred.** The banner was originally scoped here, but Studio currently only loads one game project at a time — there's no live "open workspace of multiple games" state to compare against. Implementing it against the IndexedDB-backed stored-handle list would require permission re-prompts and per-render file reads, which is the wrong shape. Re-scope it into a future epic alongside actual multi-game workspace support.

**Exit:** typing in each field persists to `gameProject.deployment.targetOverrides["google-cloud-run"]` and the next save reflects the override in the generated tfvars; clearing a field returns to the `${gameSlug}-v${majorVersion}` default and the placeholder shows that derived value; pasting `https://github.com/nikki/wordlark.git` into the GitHub Repository field stores `nikki/wordlark`.

### 45.4 — Setup Infra + Teardown Infra lifecycle

Adds the `setup-infra` and `teardown-infra` host actions to the SugarDeploy middleware (`apps/studio/vite.config.ts`), with the terraform-on-PATH precondition check, the correct cwd for both, and the ordered service-delete-before-`terraform destroy` sequencing for teardown (each `gcloud run services delete` tolerating "not found" so partial states heal). Binds the corresponding buttons in the Studio UI; surfaces stdout/stderr through the existing action-descriptor channel.

**Exit:** with the wordlark setup pointing at a fresh GCP project, clicking Setup Infra runs `terraform apply` and produces Artifact Registry, runtime SA, IAM bindings, WIF pool/provider, and empty Secret Manager containers; clicking Teardown Infra deletes any Cloud Run service first (succeeds even when none exist) then runs `terraform destroy` cleanly without dependency errors; running Teardown a second time is a no-op.

### 45.4.5 — Create GCP Project lifecycle

Adds the `create-gcp-project` host action to the SugarDeploy middleware. The action runs a sequenced `gcloud projects create <id> --name="<displayName> v<majorVersion>"` → `gcloud billing projects link <id> --billing-account=<selected>` → `gcloud services enable <list> --project=<id>` against the resolved project id from the existing Project Id override field, with each step tolerating "already exists" / "already enabled" so re-clicking is a clean no-op. The 8 APIs enabled: `run.googleapis.com`, `artifactregistry.googleapis.com`, `iam.googleapis.com`, `iamcredentials.googleapis.com`, `secretmanager.googleapis.com`, `sts.googleapis.com`, `cloudresourcemanager.googleapis.com`, `serviceusage.googleapis.com`. Adds the companion `list-billing-accounts` host action returning open billing accounts from `gcloud billing accounts list --format=json` (filtered to `open=true`); the button's onClick consumes this and auto-picks when there's exactly one open account, opens a Mantine selector modal when there are 2+, and surfaces a clear "no open billing accounts; create one at console.cloud.google.com/billing then retry" error when there are none. Project-existence is probed via `gcloud projects describe <id>` (success vs. `NOT_FOUND`); the button label flips between "Create GCP Project" and "GCP Project ready ✓" (read-only confirmation) based on probe result, refreshed on plan recomputation. The Setup Infra button (45.4) gates on the create-project probe — it disables with tooltip "Create GCP Project first" until the project exists. Pre-flight rejects project IDs that don't match GCP's globally-unique `^[a-z][a-z0-9-]{4,28}[a-z0-9]$` rule before issuing the create call. Failure modes covered by tests: project ID taken globally (clear error pointing at the Project Id override field), billing-account-not-found, billing-link-permission-denied (typically `roles/billing.user` missing), API-enable-quota.

**Exit:** opening wordlark in Studio with no `wordlark-v1` GCP project yet present surfaces a "Create GCP Project" button and a disabled Setup Infra button (tooltip points at the missing prereq). Clicking Create GCP Project with a single open billing account on the host auto-runs the full sequence; `gcloud projects describe wordlark-v1` confirms ACTIVE state; `gcloud services list --enabled --project=wordlark-v1` shows all 8 APIs enabled; the button flips to "GCP Project ready ✓" and Setup Infra unlocks. With 2+ open billing accounts, the same flow opens a selector first. Clicking Create GCP Project a second time is a no-op. Subsequently clicking Setup Infra succeeds without any manual gcloud intervention. The reverse direction — `gcloud projects delete wordlark-v1` — remains an explicit human action via the CLI (no UI inverse in this story; soft-delete window + risk of unrecoverable destruction warrant the friction).

### 45.4.6 — Plugin Host Action SDK (decouple Studio dev server from SugarDeploy)

Closes the architectural gap exposed during the 45.4.5 loader fix: Studio's `vite.config.ts` currently has hardcoded knowledge of SugarDeploy — three `createXxxPlugin()` functions naming the plugin, leaf-file imports from `packages/plugins/src/deployment/*`, and a load-bearing comment about why those imports can't go through the public barrel. Worse, the workaround for the barrel-load issue pushed plugin-derived service-name computation into the React layer, splitting the plugin's contract across the localhost POST boundary — `planGameDeployment` runs in the browser and the middleware trusts the result, so any future change to the naming derivation has to be coordinated across both sides.

This story defines a Plugin Host Action SDK and refactors 45.4 + 45.4.5 onto it. Adds a `provideHostMiddleware(): VitePlugin[]` contribution method to the plugin definition shape in `packages/plugins/src/sdk`. Adds a leaf-only registry helper in `packages/plugins/src/host/` that walks every discovered plugin definition, gathers `provideHostMiddleware()` returns, and exposes them as a single flat array consumable by `vite.config.ts`. The registry file has zero `@sugarmagic/*` value imports — only type imports — so esbuild's config-load bundling stays clean.

Moves the entire host-side surface from `vite.config.ts` into `packages/plugins/src/deployment/host/`: `createSugarDeployHostPlugin`, `createGcpBillingListPlugin`, `createGcpProjectLifecyclePlugin`, `runHostCommand`, `runHostCommandSequence`, `ensureGcloudOnPath`, `ensureTerraformOnPath`, `probeGcpProjectExistence`, the `SugarDeployActionRequest` shape, and every middleware handler. Inside that plugin tree, the middleware can call `planGameDeployment` and `getCloudRunServiceNamesForPlan` directly — they're sibling files in the same plugin, not a Vite config-loading concern — so the React side reverts to passing only `gameProject`, and the plugin contract is one-sided again. The SugarDeploy plugin definition's `provideHostMiddleware()` returns the three Vite plugins that today live in `vite.config.ts`.

Studio's `vite.config.ts` collapses to one plugin-agnostic line: `import { gatherDiscoveredHostMiddleware } from "@sugarmagic/plugins/host";` and `plugins: [react(), ...gatherDiscoveredHostMiddleware(), createMechanicsSchemaPlugin()]`. Zero references to SugarDeploy, gcloud, terraform, deployment plugin internals, or any specific plugin id. If you remove or replace the SugarDeploy plugin tomorrow, the Studio dev server still boots cleanly with no host actions registered.

**Exit:** `grep -rE "sugardeploy|google-cloud-run|terraform|gcloud" apps/studio/` finds zero matches in `apps/studio/vite.config.ts` (matches inside `apps/studio/src/plugins/catalog/sugardeploy/` are expected — that's the plugin's Studio-side UI). All 45.4 + 45.4.5 host endpoints (`/__sugardeploy/action`, `/__sugardeploy/list-billing-accounts`, `/__sugardeploy/probe-gcp-project`, `/__sugardeploy/create-gcp-project`) respond identically to before the refactor. The React-side `runAction` POST body for `teardown-infra` contains only `{ actionKind, gameProject }` — no `cloudRunServiceNames` shortcut — and the middleware recomputes the names internally via `planGameDeployment` + `getCloudRunServiceNamesForPlan`. Removing SugarDeploy from `packages/plugins/src/builtin.ts` boots Studio with no host actions and no errors. Clicking Setup Infra / Teardown Infra / Create GCP Project in a Studio with SugarDeploy enabled produces the same results as before the refactor (the underlying gcloud / terraform calls are unchanged; only the wiring moved).

### 45.4.7 — Versioned project identifier suffixes (collision-resistant project ids)

Addresses the GCP project id collision risk surfaced during 45.4.6 verification: project ids are GLOBALLY unique across all of GCP, and short game slugs like `wordlark-v1` get squatted on. The current `${identity.id}-v${majorVersion}` derivation produces a fixed string that anyone else could already own; the user then has to remember to override the Project Id field for every fresh project — a manual step that fails open (silently picks a wrong-target project if the override is forgotten and the squatter happens to share a billing account). The fix is a per-version random suffix that the game project owns and persists.

Adds a top-level `versionedProjectIdentifiers: Record<string, string>` field on `GameProject`. Keys are `v${majorVersion}` strings; values are 5-character lowercase alphanumeric (base36) suffixes generated client-side via `crypto.getRandomValues` — ~60M combinations per slot, collision probability effectively zero for a single user. Default for older projects loaded via `normalizeGameProject` is `{}` (zero-effort migration; the no-suffix path stays available for back-compat). The field is plain JSON so it round-trips through git, history, worktrees, and project file edits without ceremony.

Updates the Cloud Run overrides normalizer (`packages/plugins/src/deployment/overrides.ts` — `defaultVersionedDeploymentSlug` and the project-id / service-name-prefix derivers): when `versionedProjectIdentifiers["v${majorVersion}"]` is present, the resolved id becomes `${identity.id}-v${majorVersion}-${suffix}` (e.g., `wordlark-v1-k3m9p`); when absent, the existing `${identity.id}-v${majorVersion}` path is preserved verbatim so projects that haven't yet been touched continue to resolve the way they always have. The same derivation applies to `service_name_prefix` so terraform, deploy.sh, and gcloud all stay aligned through a single source of truth. Explicit `projectId` / `serviceNamePrefix` overrides continue to win as today.

Adds an `EnsureVersionedProjectIdentifierCommand` to the domain commands union: takes `{ majorVersion: number, suffix: string }`, idempotently sets `gameProject.versionedProjectIdentifiers["v${majorVersion}"]` IF it isn't already set (never overwrites existing entries — this matters because the historical map is what makes worktrees and `git checkout v1.0.0` round-trip correctly). The Studio SugarDeploy view's `useEffect` checks on mount whether the current major has an entry; if not, generates a suffix and dispatches the command, after which the form re-renders with the new resolved id displayed in the placeholder. Generation uses `crypto.getRandomValues(new Uint8Array(...))` mapped through base36 — purely client-side, no host action, no network.

Amends 45.8's Cut New Major Version sequence: in addition to creating the `v1.0.0` git tag and bumping `majorVersion`, the cut generates a new suffix for the new version and appends to `versionedProjectIdentifiers` *before* persisting the bump (so the resulting commit carries both the version bump and the corresponding cloud identifier). Historical worktrees carry their own historical map; a `git worktree add ../wordlark-v1-patch v1.0.0` opened in Studio resolves back to the original `wordlark-v1-${originalSuffix}` GCP project even when the current branch is on v2, v3, etc.

**Exit:** opening a wordlark whose `versionedProjectIdentifiers` map lacks a `v1` entry triggers a single-shot generation of a 5-char suffix on first SugarDeploy form mount; the suffix persists to the project file via `EnsureVersionedProjectIdentifierCommand` and survives `git status` showing the project file as modified. The placeholder in the GCP Project Id field updates to `wordlark-v1-${suffix}`. `gcloud projects describe wordlark-v1-${suffix}` returns NOT_FOUND (because the suffix is randomly generated and effectively guaranteed unique), the Create GCP Project button enables, and the existing 45.4.5 flow completes against the new id. Loading an older project file with no `versionedProjectIdentifiers` field resolves to `wordlark-v1` (no suffix) as before — the back-compat path is preserved until the user opens the SugarDeploy view, at which point generation runs. An explicit Project Id override still wins (`wordlark-myversion` stays as `wordlark-myversion` regardless of what's in the map). Re-mounting the SugarDeploy view never regenerates an existing suffix (the EnsureVersionedProjectIdentifier command is no-op when the entry exists). Unit tests cover: default empty map on missing field, generated suffix shape (`/^[a-z0-9]{5}$/`), derivation with-suffix + without-suffix paths, command idempotency, and that the override path bypasses suffix derivation entirely.

### 45.5 — Secret management + `deploy.sh` terraform-outputs refactor

Implements the `resolveSecretManagerName(plan, secretKey)` helper with the full slugification rules (lowercase, non-`[a-z0-9-]` → `-`, collapse runs, trim, `s-` prefix when digit-leading, 80-char cap, plan-time error when serviceNamePrefix and game-slug fallback both unresolvable) and routes all four call sites through it (terraform generator, deploy-script generator, `set-secret-value`, `secret-status`). Adds the Secrets subsection in the Studio UI with per-secret status display and Set Value affordance; the Set Value modal masks input, never logs, never retains the value in component state after submit. Implements the `set-secret-value` host action (value passed to `gcloud secrets versions add` via stdin; explicit log suppression on the endpoint; reject zero-length and absurdly large values) and the `secret-status` host action. Refactors `formatCloudRunDeployScript` so `deploy.sh` reads `terraform output -json` for `artifact_registry_url`, `runtime_sa_email`, and secret names, and binds secrets via `--set-secrets=KEY=SECRET_NAME:latest` instead of `--set-env-vars`. Bakes in the right-sized Cloud Run service defaults on every `gcloud run deploy`: `--cpu=1 --memory=512Mi --cpu-throttling --min-instances=1 --max-instances=4` (concurrency stays at the gcloud default of 80). Rationale: gateway is a thin HTTP proxy now that LLM + vector DB are external; `min-instances=1` keeps in-game dialog snappy with no cold start; CPU throttling collapses idle billing by ~10×. Past learning: the predecessor wordlark project shipped at `cpu=4, memory=8Gi, min-instances=1, --no-cpu-throttling` (sized for in-container model + vector DB inference) and burned ~$300/mo of idle compute per service — these defaults are the explicit fix. The whole story moves together — splitting any of the four pieces would leave the naming source-of-truth mid-transition.

**Exit:** for each declared secret, the Studio shows Not Set; clicking Set Value, entering a value, and submitting flips it to Set with a version id timestamp; the value never appears in any Studio log, browser console, or shell history (verified by the lint/test described in Deliverables → Tests); clicking Deploy builds and pushes the gateway image and the deployed service references each secret via `--set-secrets`; the running service can read each secret at runtime (verified by hitting a debug endpoint that echoes a presence flag, not the value); `gcloud run services describe` on the deployed service confirms `spec.template.spec.containers[0].resources.limits` = `{cpu: "1", memory: "512Mi"}` and `metadata.annotations` carry `autoscaling.knative.dev/minScale=1`, `autoscaling.knative.dev/maxScale=4`, and **no** `run.googleapis.com/cpu-throttling=false` annotation (i.e., throttling is left at the default-enabled).

### 45.5.5 — Baseline `sugarmagic-gateway` service unit (always deployable)

Closes a gap surfaced during 45.5 verification: with zero plugins enabled that contribute a shared-allowed request-response runtime-service requirement, `buildServiceUnits` produces zero service units, the deploy.sh `services=()` array is empty, and there's nothing to build + push. That makes the entire deploy pipeline unverifiable until the user has committed to a plugin (with its secrets, routes, dependencies). Conceptually it's also weird — the gateway is a property of the deployment, not of any individual plugin's runtime-service-requirement.

The planner now injects a baseline `sugarmagic-gateway` service unit whenever `buildServiceUnits` would otherwise return an empty list AND a deployment target is selected. The baseline carries `ownerIds: ["sugardeploy"]`, no proxy routes, no secrets — just enough metadata for the existing scaffold generators (`buildGatewayServerFile`, `buildGatewayDockerFile`, `buildGatewayPackageFile`) to emit a minimal Node container. The existing `server.mjs` template exposes `/health` via `if (path === "/" || path === "/health") sendJson(res, 200, { ok: true, target, serviceUnitId, routes })`, so the baseline has a useful response surface from day one. (Discovered the hard way during 45.5.7 verification: Cloud Run's Google Front End reserves `/health` platform-wide and returns Google's generic 404 page before the request reaches the container. `/health` is namespaced under a sugarmagic-specific prefix so no Google frontend shadows it.)

When a plugin (SugarAgent, etc.) declares a shared-allowed request-response runtime-service requirement, the existing bucket logic produces the gateway unit and the baseline injection is skipped — no duplication, no fallback when there's a "real" contributor. Plugins that contribute proxy-routes flow through the unit's `proxyRoutes` field and become routes on the same gateway. This preserves the "one gateway per game, plugins merge in" model.

**Exit:** opening wordlark with zero plugins enabled (other than the always-on shell, with Cloud Run selected as the target) shows a single service unit (`sugarmagic-gateway`) in the Managed Files section; the generated `services/sugarmagic-gateway/server.mjs` exposes `/health`; saving + Setup Infra + Deploy stands up the Cloud Run service `wordlark-v${N}-${suffix}-sugarmagic-gateway`; `curl https://<service-url>/health` returns `200 OK` with `{ ok: true, target, serviceUnitId: "sugarmagic-gateway", routes: [] }`. With SugarAgent then enabled and re-saved, `serviceUnits` still has exactly one entry (the bucket logic produces `sugarmagic-gateway` with SugarAgent's `ownerIds` + proxy routes + secrets), and a redeploy adds SugarAgent's routes to the same running service without changing the URL.

### 45.5.7 — Public reachability + gateway auth-mode placeholder

Closes a verification gap surfaced during 45.5.5: the gateway was deployed and our code was running, but no caller could reach it. The foxleapmoon org policy `iam.allowedPolicyMemberDomains` (set 2025-04-02) restricts IAM bindings to a single customer-id allowlist, so `--allow-unauthenticated` on `gcloud run deploy` silently fails to bind `allUsers → roles/run.invoker` and the Cloud Run frontend rejects every request as unauthorized. Old wordlark-490602 was set up before that policy and got a permanent grandfather binding; new projects can't.

The infrastructure-side fix is small and automatable: terraform creates a project-level override on `constraints/iam.allowedPolicyMemberDomains` (`allow_all = TRUE`), then binds `allUsers → roles/run.invoker` at the PROJECT level (service-level binding isn't possible here because the Cloud Run service name is generated at `gcloud run deploy` time and not knowable to terraform). The override requires `roles/orgpolicy.policyAdmin` at the org level for the deploying principal; that's nikki for foxleapmoon. When a future user doesn't have that role, terraform surfaces a clear permission-denied error during Setup Infra that names exactly which role to request.

The app-layer fix is *deliberately deferred* to a separate epic. Adds a `gatewayAuthMode: "none"` field to `GoogleCloudRunDeploymentTargetOverrides`, surfaced in the Studio Cloud Run section as a readonly field with explainer copy and in the generated README. For now the only valid value is `"none"` and the generated `server.mjs` runs in no-auth mode (no Fastify, no JWT validation, no cookies — just the existing minimal `node:http` server with `/health`). When [Plan 047](047-identity-provider-plugin-model-epic.md) lands, the enum expands to include identity-provider plugin ids (`"supabase"`, `"auth0"`, etc.); each one contributes its own validation middleware into the generated server.mjs and declares its own deployment secrets that flow through the existing Set Value modal.

`CLOUD_RUN_TEMPLATE_VERSION` bumps from 1 to 2 because the resource graph changed; existing projects see the template-drift banner (when 45.7 lands) and re-save to pick up the new resources. `orgpolicy.googleapis.com` joins `REQUIRED_GCP_APIS` so Create GCP Project enables it; without that API the org-policy override resource fails to apply.

**Exit:** with the wordlark deploy from 45.5.5, re-saving + re-running Setup Infra creates the `google_org_policy_policy.allow_public_invokers` resource (only — the project-level `allUsers → run.invoker` binding originally planned here is invalid: GCP rejects project-scoped `allUsers` bindings with `PROJECT_SET_IAM_DISALLOWED_MEMBER_TYPE`. The actual SERVICE-level binding happens via deploy.sh's `--allow-unauthenticated` flag, which now succeeds because the org policy override is in place); a subsequent Deploy of the baseline gateway succeeds without IAM warnings; `curl https://<service-url>/health` from any unauthenticated client returns `200 OK` with `{ ok: true, target, serviceUnitId: "sugarmagic-gateway", routes: [] }`. The Cloud Run section of the Studio form shows a readonly "Gateway Auth Mode: none" field with explainer copy pointing at Plan 047. The generated README's Google Cloud Run Notes section calls out the no-auth status, explicitly names the budget risk for plugin routes that cost money (LLM proxy, etc.), and points at Plan 047 for the future shape. Setup Infra fails clearly with a permission-denied error when the deploying principal lacks `roles/orgpolicy.policyAdmin` at the org level, naming the missing role.

### 45.5.7 lessons-learned annex (not story scope; reference for future-self)

Several rabbit holes during 45.5.7 verification turned into permanent code or task items. Captured here to avoid re-discovering them:

- **`/healthz` is Cloud Run reserved.** GFE intercepts it before reaching the container and returns Google's generic 404 page (no `x-cloud-trace-context`). Gateway uses `/health` instead. Affects the old wordlark service too; we never noticed because nothing curled it from outside.
- **Project-level `allUsers → run.invoker` IAM binding is rejected by GCP.** Project-scoped `allUsers` bindings hit `PROJECT_SET_IAM_DISALLOWED_MEMBER_TYPE`. The terraform resource was dropped; the service-level binding flows through `gcloud run deploy --allow-unauthenticated` instead.
- **ADC quota project mismatch breaks new-project terraform applies.** Fixed at the terraform provider level via `user_project_override = true` + `billing_project = var.gcp_project_id` rather than telling the user to `gcloud auth application-default set-quota-project` per project.
- **`roles/orgpolicy.policyAdmin` is required to create the org-policy override.** Baked into the create-gcp-project orchestration as a step after API enablement; the host action grants it to the current user via `gcloud organizations add-iam-policy-binding`. Falls through with a clear error if the user lacks `resourcemanager.organizationAdmin`.
- **The Create GCP Project button is locked in "ready ✓" state** so newly-added enable-API / IAM steps in the orchestration sequence can't be applied to existing projects. Captured as task #63; will resolve in 45.8.5.
- **Granular feedback memories landed during 45.5.7:** `feedback-no-flakey-tests`, `feedback-check-code-before-asking`, `feedback-sugarmagic-orchestrates-everything`, `feedback-no-over-explain`.

### 45.5.8 — Bearer-token gateway auth mode (interim, pre-Plan-046)

Closes the immediate budget-risk gap exposed by 45.5.5 + 45.5.7: SugarAgent's routes are exposed, anyone with the URL can call them, and every call costs money. Plan 047's proper identity-provider plugin model is too big to ship before this becomes a real problem, so 45.5.8 lands the simplest auth shape that's actually effective for solo-dev alpha: a single deployment-wide shared bearer token.

The `GatewayAuthMode` enum (introduced in 45.5.7 with only `"none"` valid) expands to `"none" | "bearer"`. The Studio form's gatewayAuthMode field flips from readonly to a `Select` with two options. When `gatewayAuthMode === "bearer"`, the planner injects a `gateway-shared-token` `SecretRequirement` (mappingHint `SUGARMAGIC_GATEWAY_SHARED_TOKEN`, consumption `server-only`, exposure `private`, ownerId `sugardeploy`) into the gateway service unit's `secrets` list — which flows through every downstream path the existing 45.5 secret machinery established: terraform creates the container, `collectSecretEnvBindings` produces a `--set-secrets` binding, deploy.sh's `gcloud run deploy` invocation gets the env var wired, and the Studio's Secrets section shows a Set Value affordance.

The generator emits an `authorizeBearer(req)` helper in the baseline `server.mjs` template (via `import { timingSafeEqual } from "node:crypto"` for the constant-time compare). When `gatewayAuthMode === "bearer"`, an explicit gate runs AFTER the public `/health` check but BEFORE the route dispatcher: any request without a valid `Authorization: Bearer <token>` header returns 401 immediately, never reaching the plugin route handlers. Length-mismatched tokens short-circuit before the constant-time compare (which requires equal-length buffers); missing env var (e.g. bearer mode was selected but the secret value hasn't been written yet) denies every gated request. The generated README's Google Cloud Run Notes section expands in bearer mode to explain the header shape, the rotation path (set a new value via Set Value; previous version is retained in Secret Manager but only `:latest` is bound at deploy time), and the explicit caveat that this is a single shared token across all callers — NOT per-user identity — and that real identity ships in Plan 047.

**Exit:** flipping the wordlark Gateway Auth Mode to "bearer" in the Studio form and saving regenerates terraform with the new secret container in the `secret_names` list and regenerates the baseline `server.mjs` with the `authorizeBearer`-gated request handler. After Setup Infra (creates the new container) + Set Value (writes a random 32-byte token) + Deploy, `curl https://<service-url>/health` still returns the existing 200 OK JSON, but `curl https://<service-url>/<any-plugin-route>` returns 401 with `{ ok: false, error: "Unauthorized", message: "..." }` unless the request includes `Authorization: Bearer <correct-token>`. With the correct token, the plugin route resolves as before. Flipping back to "none" and re-deploying restores the public-access behavior. Setting the bearer mode but leaving the token unset causes EVERY non-/health request to 401, even with a header (because the gateway has nothing to compare against) — the absence of the token IS the gate.

### 45.6 — Health + Stop symmetry across targets

Upgrades both targets' `health` host action to a real HTTP probe per the Deliverables spec: `fetch(healthUrl, { signal: AbortSignal.timeout(5000), redirect: "manual" })`, returns `{ ok, statusCode, durationMs, bodyPreview, error? }`, body truncated to 512 bytes, no retries, no redirect follow. Surfaces the result through the existing action descriptor's `stdout` field. For Cloud Run, resolves the service URL via `gcloud run services describe <service-name> --format='value(status.url)'` once per probe and short-circuits with `supported: false` if the service isn't deployed. For local, probes `http://localhost:<gatewayHostPortBase>/health` directly. Implements `stop` for Cloud Run as `gcloud run services delete <service-name> --quiet --project=<projectId> --region=<region>`, idempotent.

**Exit:** clicking Health against a running Cloud Run wordlark deployment returns `{ ok: true, statusCode: 200, durationMs, bodyPreview }`; clicking Stop deletes the Cloud Run service but leaves Artifact Registry, IAM, WIF, and Secret Manager state intact; a subsequent Deploy brings the service back without re-running Setup Infra; clicking Health against the local target returns the same shape; clicking Stop with the service already gone returns a clear "already deleted" message.

### 45.7 — Template-drift banner

Adds the non-blocking template-drift banner described in Deliverables → Studio UI. When `readTemplateVersionFromDisk(gameRoot)` (from 45.2) returns a version strictly less than the plugin's current template version, the banner renders above the action buttons; it clears automatically on the next successful save or Setup Infra run because the on-disk stamp updates.

**Exit:** hand-edit the template-version stamp in wordlark's generated `main.tf` to `# SUGARMAGIC TEMPLATE VERSION: 0`; re-opening the project surfaces the banner; saving the project regenerates with the current stamp and the banner clears without a manual reload.

### 45.7.5 — SugarDeploy plugin-state migration (contractor-test prerequisite)

Moves all SugarDeploy-shaped fields off the core `GameProject` type and into the SugarDeploy plugin's namespaced state slot, so that uninstalling SugarDeploy leaves zero contamination on the domain type. Contractor test: deleting `packages/plugins/src/catalog/sugardeploy/` should leave the domain typechecking clean, and a developer should still be able to author and locally preview a game without the deploy plugin installed (sans SugarAgent, which depends on it).

**What moves off `GameProject`:**
- `deployment: DeploymentSettings` → `pluginConfigurations[id="sugardeploy"].config.settings`
- `versionedProjectIdentifiers: Record<string, string>` → `pluginConfigurations[id="sugardeploy"].config.versionedProjectIdentifiers`

**What stays on `GameProject`:**
- `majorVersion: number` — legitimate game-authoring primitive (save-game compatibility, changelogs, in-game UI display). Not tied to deployment.

**What gets deleted from domain commands:**
- `UpdateDeploymentSettingsCommand` — replaced by `UpdatePluginConfiguration` writing to the deploy plugin's `config` slot.
- `EnsureVersionedProjectIdentifierCommand` — same. The deploy plugin computes the new config locally and dispatches the generic `UpdatePluginConfiguration` command.
- Per the architecture principle of "bias toward deletion of legacy paths," the field declarations are removed from `GameProject` immediately; the normalize step reads old-shape project files (top-level `deployment` and `versionedProjectIdentifiers`) and writes the new shape on next save. After one save the legacy slots no longer appear in `project.sgrmagic`.

**What the SugarDeploy plugin contributes (typed accessor surface):**
- `getDeploymentSettings(gameProject): DeploymentSettings` — reads from `pluginConfigurations[id="sugardeploy"].config.settings`, falling back to defaults when the plugin isn't enabled or no config is present.
- `getVersionedProjectIdentifiers(gameProject): Record<string, string>` — same source, defaults to `{}`.
- `buildUpdateDeploymentSettingsCommand(gameProject, settings): UpdatePluginConfigurationCommand` — single helper that builds the right command payload so the Studio side never hand-rolls plugin-config patches.
- `buildSetVersionedProjectIdentifierCommand(gameProject, majorVersion, suffix): UpdatePluginConfigurationCommand` — same shape for the suffix-register operation, preserving the idempotence guarantee from the old `EnsureVersionedProjectIdentifierCommand` (writes only if absent).

**Back-compat read path:** `normalizeGameProject` accepts either shape on input — old projects with top-level `deployment` + `versionedProjectIdentifiers` get migrated into the plugin-config slot during normalize. The output shape is always the new one. This is a forward-only migration; once a project saves under the new shape there is no need to keep emitting the legacy fields.

**Exit:** the `GameProject` interface in `packages/domain/src/game-project/` contains neither `deployment` nor `versionedProjectIdentifiers`; grep across `packages/domain/` returns zero hits for those two identifiers and zero hits for `UpdateDeploymentSettings` or `EnsureVersionedProjectIdentifier`. All SugarDeploy reads in the plugin and the Studio workspace UI go through `getDeploymentSettings(gameProject)` / `getVersionedProjectIdentifiers(gameProject)`. All SugarDeploy writes dispatch the generic `UpdatePluginConfiguration` command via the new builder helpers. Opening wordlark on the old project.sgrmagic shape loads cleanly; saving rewrites the file with the new shape; re-opening confirms idempotence. The contractor test passes: deleting `packages/plugins/src/catalog/sugardeploy/` and `packages/plugins/src/deployment/` from the workspace leaves the domain package typechecking clean (because no domain code references SugarDeploy types or commands) and the Studio still opens a project with the SugarDeploy plugin uninstalled (the workspace tab is absent; everything else works). All existing tests (testing/plugin-infrastructure.test.ts and friends) continue to pass after adjusting their fixtures to the new shape.

### 45.8 — Cut New Major Version

Implements the cut-major-version operation as a Studio-orchestrated saga inside the SugarDeploy plugin. Three small host actions in `packages/plugins/src/catalog/sugardeploy/host/middleware.ts` plus a single new domain command (`BumpMajorVersion`) compose the user-facing one-button flow.

**Saga shape** (each owner does what it owns: host shells out to git, domain owns project state, IO owns file writes through the existing command bus):

1. `prepare-cut-major-version` (host action): verifies git on PATH, working tree clean, and that `v{currentMajor}.0.0` does not already exist. Generates the next suffix client-side via `crypto.getRandomValues` and returns `{ newMajorVersion, newSuffix, newProjectId }` for the modal preview. No side effects.
2. User confirms the Release New Version modal (preview matches the prepared plan).
3. `tag-prior-major` (host action): re-verifies pre-flight (servers do not trust UI disable state), then runs `git tag v{prior}.0.0 HEAD`. First step with a real side effect.
4. `BumpMajorVersion` (new domain command, dispatched by Studio): atomically bumps `majorVersion` AND writes the new suffix into the SugarDeploy plugin config slot's `versionedProjectIdentifiers` map (via the `buildSetVersionedProjectIdentifierCommand` helper from 45.7.5 — internally a single `UpdatePluginConfiguration` payload that carries both). Persists `project.sgrmagic` via the existing IO command bus.
5. `commit-major-version-bump` (host action): `git add project.sgrmagic && git commit -m "chore: bump major version to N"`.

Push is **not** automatic.

**Rollback** (Studio-side orchestrator):

| Fails at step | Undo sequence |
|---|---|
| 1 (prepare) | nothing — no side effects yet |
| 3 (tag) | nothing — the tag step itself is the side effect; failure leaves no partial state |
| 4 (bump) | `git tag -d v{prior}.0.0` |
| 5 (commit) | dispatch inverse `BumpMajorVersion` (rollback project state), then `git tag -d v{prior}.0.0` |

If any undo step fails its own rollback path, the orchestrator surfaces an escalated error message instructing the user to inspect `git tag -l` and the project's `pluginConfigurations` entry by hand. (Solo-dev escape hatch; the happy path covers the realistic failure modes.)

**Confirmation modal copy** previews: the tag that will be created, the new `majorVersion`, the new resolved `gcp_project_id` (`${slug}-v{new}-${newSuffix}`), and the literal commit message; states explicitly "this commits to git locally; push is not automatic." Button is enabled only when the prepare host action returned a clean pre-flight; otherwise disabled with the prepare action's `reason` text in the tooltip.

**Exit:** with wordlark on `majorVersion: 1` and a clean working tree, clicking Release New Version walks the saga to success — produces a `v1.0.0` git tag at the prior HEAD, bumps `majorVersion: 1 → 2`, registers `versionedProjectIdentifiers["v2"]` with a fresh 5-char suffix inside the plugin config slot (NOT on `GameProject` top-level), and creates a `chore: bump major version to 2` commit. The SugarDeploy section then resolves the default project id to `wordlark-v2-{suffix}` and the Create GCP Project button re-enables (since the new id does not exist in GCP yet). Clicking Create GCP Project (45.4.5) → Setup Infra (45.4) → Deploy (45.5) stands up v2 in a fresh GCP project with zero manual gcloud. The v1 GCP project keeps running untouched. `git worktree add ../wordlark-v1-patch v1.0.0` followed by opening that worktree in Studio shows `majorVersion: 1`, SugarDeploy resolves back to the original v1 GCP project (because the v1 suffix lives in that worktree's plugin config slot, never deleted), and Create GCP Project is in its "ready ✓" no-op state. The unit test suite covers happy path, dirty-tree refusal, existing-tag refusal, missing-git refusal, and each rollback path in the table above.

### 45.8.5 — SugarDeploy lifecycle UX overhaul (PLACEHOLDER — to be shaped after 45.6)

**Status:** placeholder. The motivation is captured here so we don't lose it; the actual scope is intentionally deferred until after 45.6 lands so we can iterate against the working deploy+health+stop loop instead of in the abstract.

**The honest critique driving this story.** After end-to-end-verifying 45.4 + 45.4.5 + 45.4.6 + 45.4.7 in one session, the SugarDeploy workspace shape is bad enough to write down. Specific failure modes nikki called out, all of which are real:

1. **Buttons are a grab-bag.** Create GCP Project, Setup Infra, Teardown Infra, Deploy, Status, Stop, Cut New Major Version all live in a single horizontal row with no grouping, no precedence, and no story about what to click when. The lifecycle ordering (provision project → provision infra → deploy → operate → cut new major) is invisible.
2. **Bumping a major version isn't a discoverable flow.** Once 45.8 lands, Cut New Major Version IS a button, but it's just another button in the grab-bag. New users won't know that cutting a version flips the active GCP project to a fresh one, that they then need to re-Create GCP Project, then re-Setup Infra, then Deploy, etc. The whole sequence needs to be a guided journey, not "click and figure out."
3. **No deployment history surface** (the original scope of this story). Current settings live in `targetOverrides`, v1-era settings live in a git tag, the `v${major}` → `${suffix}` map lives in `versionedProjectIdentifiers`, and the running GCP projects keep running side-by-side until explicitly deleted. "What was v1's region?" requires a git worktree dance; "is the v1 service still running?" requires gcloud; nothing in Studio gives a glance-level summary.
4. **Discoverability for future-self.** "When I come back to this in a month and I don't remember anything and Claude doesn't remember anything, will I be able to figure out how to ship a v2 from scratch?" — currently no, because the implicit ordering is folklore in chat logs and plan stories, not visible in the UI.

5. **Lifecycle phase buttons go invisible once their phase is "done," even though the underlying action is idempotent.** Concrete instance from the 45.5 verification: the Cloud Build API was missing from `REQUIRED_GCP_APIS`. Adding it to the list made the new entry land in the next call to the `create-gcp-project` middleware — but the React side hides the button once `probeState.status === "owned"`, so there's no in-UI path to trigger the new API enable. nikki had to drop to `gcloud services enable cloudbuild.googleapis.com --project=...` from her terminal. The buttons should adapt their copy and stay visible (e.g., "Re-apply Project Config" / "Verify APIs"), since the middleware happily handles re-runs.

**Target shape** (sketch, subject to revision once we live with 45.6 for a bit). Two big moves:

**Move 1 — Promote the deployment list to first-class.** AWS-task-definition-style: one row per major version that has ever been cut, ordered newest first. Each row shows the major version label, the resolved GCP project id, an at-a-glance running status (queried via the `status` host action), the region, and the cut date. A "▸ View settings" toggle expands the row to show that version's deployment settings as a read-only snapshot. The current major is flagged "(active)". The "Cut New Major Version" action lives at the top of this list section, visually framed as "produce the next entry."

**Move 2 — Group the operational buttons by lifecycle phase, with phase ordering and gates.** Rough sketch:
  - **Phase 1: Provision** (only enabled when the current major's GCP project doesn't exist): `Create GCP Project` → `Setup Infra`. Gated sequentially; Setup Infra disabled until Create passes (already true, just made visually explicit).
  - **Phase 2: Deploy** (enabled when Phase 1 has passed): `Set Secret Value` (per-secret, from 45.5) → `Deploy`. The secret modal blocks Deploy if any required secret is Not Set.
  - **Phase 3: Operate** (enabled when Phase 2 has produced a running service): `Status`, `Health`, `Stop`.
  - **Phase 4: Teardown** (always available, red, behind confirmation): `Teardown Infra`. Visually separated so it's not accidentally clicked.
  - The whole sequence gets a phase indicator at the top ("you are in Phase 2: Deploy") so the journey is visible.

**Move 3 — Group the form fields by which lifecycle phase the change targets, with explicit per-group labels.** Today's form is a flat list and there's no surface signal that `Region` is destructive once Setup Infra has run while `Min Instances` is freely editable and rolls out on the next Deploy. New grouping, each section explicitly captioned with "what does saving this trigger":
  - **Identity (changes target a NEW GCP project).** GCP Project Id override (advanced — almost always blank, auto-derived from `${slug}-v${major}-${suffix}`). Major version (display only; bumped via Cut New Major Version). Caption: *"Changes here imply a new major version + new GCP project. Use Cut New Major Version instead of editing directly."*
  - **Infrastructure (changes apply on next Setup Infra; locked after first Setup Infra unless explicitly unlocked).** Region. Service Name Prefix override. GitHub Repository. Runtime Service Account Name override. Caption: *"Changes here re-shape the supporting GCP infrastructure. After Setup Infra has run, editing these will require Teardown Infra + Setup Infra to take effect; the fields lock to prevent silent destructive edits. Unlock via an explicit confirmation."*
  - **Runtime (changes apply on next Deploy).** Container Port. Min/Max Instances. CPU. Memory. CPU Throttling. Ingress. Allow Unauthenticated. Caption: *"Changes here become live on the next Deploy — Cloud Run creates a new revision with the new shape and atomically shifts traffic to it. Safe to edit any time."*
  - **Workspace (not GCP-side).** Working Directory. Caption: *"Local-only — where the generated deploy files live on your disk."*
  Together with Move 2's phase indicator, this makes the question "if I change this field, what do I have to click next to make it effective?" answerable at a glance instead of by reading the plan doc.

**Data-model fork for the history list** (TBD during scope-out). Two viable models:
- **A) Snapshot persistence** — add `deploymentSnapshots: Array<{majorVersion, suffix, cutAt, settings}>` to GameProject; Cut New Major Version captures the current settings as an immutable entry before bumping. Simple to render; risk of drift if the user retroactively edits past entries (would need to lock them).
- **B) Git-driven derivation** — the list-view middleware reads each `v*.0.0` git tag at view time (`git show v1.0.0:game-root/project.sgrmagic` → parse → render), so the snapshots are *derived* from history rather than stored in two places. Single source of truth; slower; requires git in the host environment.
Lean toward (B) for architectural cleanness once 45.6 is in and we know what "view a snapshot" actually needs to show.

**Discoverability sub-thread.** Phase indicators + button gating handle "what do I do next." A "Getting Started" panel that appears when no deployment has ever been cut handles "I don't remember anything from a month ago" — three steps: pick a target, click Create GCP Project, click Setup Infra. Inline rationale next to each button: "This creates the GCP project the service will run in. You'll do this once per major version."

**Exit (provisional, will be replaced when scoped):** opening wordlark with two cut versions present (v1.0.0 and v2.0.0 git tags; both entries in `versionedProjectIdentifiers`) shows a two-row deployment list ordered newest first, each row's running-status indicator reflects current Cloud Run state, expanding a row shows that version's frozen settings, the Cut New Major Version action lives at the top of the list. The operational buttons are grouped by phase with cross-phase ordering enforced (you can't Deploy before Setup Infra has passed). A first-time visit (no GCP project yet) shows a Getting Started panel that walks the four phases inline.

### 45.9 — Documentation pass

Writes ADR 017 capturing the architectural decisions (plugin-owned terraform; `gcloud`-owned Cloud Run service; Studio-driven secret values; one-project-per-game-per-major-version-per-env; version-namespaced project naming; template-version + `moved {}` discipline). Updates `packages/plugins/src/deployment/README.md` with the template-version + `moved {}` discipline, the `TERRAFORM_RENAME_LEDGER` convention, and the secret naming rules. Updates the plugin-generated `deployment/google-cloud-run/README.md` to cover the full lifecycle and the Cut New Major workflow. Updates Plan 021 with a forward-reference to this epic and to ADR 017.

**Exit:** ADR 017 exists at `docs/adr/017-*.md` and is internally consistent with Plan 045; `packages/plugins/src/deployment/README.md` reflects current behavior including the `TERRAFORM_RENAME_LEDGER` and `resolveSecretManagerName` rules; the plugin-generated `deployment/google-cloud-run/README.md` in a fresh wordlark save reflects the version-namespaced project model and the Cut New Major workflow; Plan 021 carries a "Cloud Run implementation: see Plan 045 and ADR 017" cross-reference at the top.

## Resolved Decisions

- **`terraform.tfvars` is fully managed.** Plugin emits it with the `GENERATED BY SUGARMAGIC` header on every save; hand-edits get overwritten. Rationale: the Studio is the single input surface for all terraform variables. An unmanaged `terraform.tfvars` (the common terraform convention) is a tempting hiding spot for API keys — leaving it unmanaged would create a hole that bypasses the `setSecretValue` host action's no-log discipline and routes secrets through `terraform.tfstate` instead of Secret Manager. Fully managed closes that hole.

- **Template-version drift handling is uniform regardless of gap size.** The plugin always emits the correct historical `moved {}` chain (walked from the on-disk version up to current via the `TERRAFORM_RENAME_LEDGER`), and the Studio shows a non-blocking drift banner when on-disk version < current. There is no separate "N-2 or older" branch — N-1 and N-5 take the same path. Rationale: "refuse to operate" is incoherent when the plugin *is* the migration tool; "silent fix-up" can mask state drift in the solo-dev / local-state setup this epic targets. Notify + proceed gives the user something to verify (`terraform plan`) and matches AGENTS.md's "goals must be verifiable" rule. The ledger retains entries indefinitely for v1; a future plan may cap retention once the version count makes generated terraform unwieldy.

- **Cross-game GCP-project enforcement is documentation, not plan-layer validation or a runtime banner (for v1).** The plugin-generated `deployment/google-cloud-run/README.md` and the SugarDeploy workspace copy state explicitly that one GCP project per game per environment is the supported model. The originally-planned non-blocking Studio warning banner was **deferred during 45.3 implementation**: Studio currently loads one game project at a time; cross-game detection would require parsing every IndexedDB-stored project's `project.sgrmagic` on render, which is the wrong shape. Re-scope the banner into a future epic alongside actual multi-game workspace support. Rationale: plan-layer validation requires the plugin to know about every other game's deployment overrides at save time — cross-game state-awareness in a plugin that's otherwise game-scoped. The actual failure mode (`terraform destroy` from game A nuking IAM game B depends on; `gcloud projects delete` killing both at once) is solo-dev foot-gun territory mitigated by documentation alone for v1.

- **Game-project versioning is `majorVersion` metadata + git tags + version-namespaced GCP projects, with a first-class Cut New Major Version button.** One game directory per game forever; git is the source of truth for what code is at what version; the metadata field tells the deployment plugin which version slot the current working tree represents; the plugin derives the default `gcp_project_id` and `service_name_prefix` from `${gameSlug}-v${majorVersion}` so coexisting major versions land in isolated cloud projects automatically. The Studio's Cut New Major Version button shells the tag + bump + commit sequence so the operation is one click, not a checklist. Rationale: the directory-suffix-as-version approach (`wordlark/`, `wordlark-v2/`, etc.) is a copy-the-whole-project crutch that duplicates source, makes cross-version bug fixes a cherry-pick chore, and breaks the "one source of truth" rule from AGENTS.md. Git already solves the "what code is at this version" problem; the metadata mirror exists only so the deploy plugin can target the right cloud slot without round-tripping through `git describe`. Minor/patch versioning is intentionally out of scope for v1 — the only version distinction that affects deploy slotting is the major.

- **SugarDeploy state lives in the plugin's namespaced `pluginConfigurations[].config` slot, never on `GameProject` directly.** Story 45.7.5 moves `deployment: DeploymentSettings` and `versionedProjectIdentifiers: Record<string, string>` off the core `GameProject` type and into the SugarDeploy plugin's config slot (an architectural primitive that already exists for every plugin via `PluginConfigurationRecord.config`). Domain mutations route through the generic `UpdatePluginConfigurationCommand` rather than deploy-named domain commands. Rationale: the contractor test — "could we hand this codebase to someone and ask them to write a brand-new deploy plugin from scratch, and would the existing API be clean to that contractor?" — fails as long as deploy-shaped fields live on the domain type. `versionedProjectIdentifiers` is unambiguously a GCP suffix map; if SugarDeploy is uninstalled, dangling random strings persist on the domain object with no code to interpret them. The fix is forward-only (back-compat read at normalize time, then the legacy fields go away on next save) and the existing `pluginConfigurations[].config` slot lets us do it without inventing a new contract surface. Same call for `majorVersion`: it STAYS on `GameProject` because a game has a major version regardless of where it ships (save-game compat, changelog, in-game UI), but the suffix map and deployment settings are pure SugarDeploy concerns.

## Open Questions

_(All design open questions for v1 are resolved. Implementation will likely surface new ones; record them here as they appear.)_

## Builds On

- [Plan 021: Deployment Plugin and Publish/Deploy Target Architecture Epic](/Users/nikki/projects/sugarmagic/docs/plans/021-deployment-plugin-and-publish-deploy-target-architecture-epic.md)
- [ADR 009: Game Root Contract](/Users/nikki/projects/sugarmagic/docs/adr/009-game-root-contract.md)
- [Proposal 005: Sugarmagic System Architecture](/Users/nikki/projects/sugarmagic/docs/proposals/005-sugarmagic-system-architecture.md)
- [AGENTS.md — Non-Negotiable Principles](/Users/nikki/projects/sugarmagic/AGENTS.md)
