/**
 * Web-side symmetry export for runtime UI binding resolution.
 *
 * The implementation lives in runtime-core so Studio preview and the running
 * target cannot drift on authored binding semantics.
 */

export { resolveBinding } from "@sugarmagic/runtime-core";
