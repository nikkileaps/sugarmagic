node_modules
dist
coverage
.DS_Store
.vite
*.tsbuildinfo
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
*.pyo

*.rar

# Playwright MCP working directory — console logs, page snapshots, CSV exports
.playwright-mcp/

# Perf-probe run output: a measurement of one machine on one day, not
# something to carry in the repo. The threshold it produced lives in a
# comment on MAX_COMFORTABLE_PLACED_LIGHTS.
packages/perf-harness/light-budget.json
