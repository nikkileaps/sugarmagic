/**
 * Runtime-core render boundary.
 *
 * runtime-core no longer owns Three/WebGPU render implementation. Shared web
 * rendering lives in @sugarmagic/render-web; runtime-core keeps pure render
 * semantics elsewhere.
 */

export {};
