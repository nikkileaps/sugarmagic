# Schema Stubs

This directory holds the JSON Schema placeholders for sugarlang data and persistence surfaces.

Epic 1 keeps these schemas intentionally minimal so the file ownership is locked in before the real fields are designed. The intended owners are:

- `cefrlex.schema.json`: Epic 4
- `learner-profile.schema.json`: Epic 7
- `scene-lexicon.schema.json`: Epic 6
- `placement-questionnaire.schema.json`: Epic 11
