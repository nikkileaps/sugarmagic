/**
 * packages/plugins/src/catalog/sugarlang/tests/budgeter/scoring.test.ts
 *
 * Purpose: Reserves the Budgeter scoring test file owned by Epic 8.
 *
 * Exports:
 *   - none
 *
 * Relationships:
 *   - Imports ../../runtime/budgeter/scoring to keep the module path stable.
 *   - Will hold scoring-rule tests once Epic 8 lands.
 *
 * Implements: Proposal 001 §1. Lexical Budgeter
 *
 * Status: skeleton (no implementation yet; see Epic 8)
 */

import "../../runtime/budgeter/scoring";
import { describe, it } from "vitest";

describe("TODO: Epic 8", () => {
  it.todo("implement scoring tests in Epic 8");
});
