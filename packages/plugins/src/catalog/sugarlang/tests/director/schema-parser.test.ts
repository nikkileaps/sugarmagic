/**
 * packages/plugins/src/catalog/sugarlang/tests/director/schema-parser.test.ts
 *
 * Purpose: Reserves the Director schema-parser test file owned by Epic 9.
 *
 * Exports:
 *   - none
 *
 * Relationships:
 *   - Imports ../../runtime/director/schema-parser to keep the module path stable.
 *   - Will hold schema-parser tests once Epic 9 lands.
 *
 * Implements: Proposal 001 §3. Director
 *
 * Status: skeleton (no implementation yet; see Epic 9)
 */

import "../../runtime/director/schema-parser";
import { describe, it } from "vitest";

describe("TODO: Epic 9", () => {
  it.todo("implement schema-parser tests in Epic 9");
});
