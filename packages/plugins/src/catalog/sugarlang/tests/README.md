# Sugarlang Tests

This directory holds sugarlang-local tests.

- Unit tests live under `tests/<component>/`.
- Integration-oriented plugin tests live under `tests/integration/`.
- Cross-plugin or workspace-spanning tests belong in `packages/testing/` when they need shared fixtures outside the plugin boundary.

Epic 1 keeps every test except plugin registration as a placeholder `todo`.
