# `packages/workspaces/src/design/game-ui`

Studio Design workspace for project-owned game UI.

This workspace edits `MenuDefinition`, `HUDDefinition`, and `UITheme` through
domain semantic commands. Its center preview is supplied by Studio and embeds a
target preview session instead of rendering its own fake UI.
